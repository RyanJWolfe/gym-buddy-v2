<%# locals: ( form: ) -%>

<%= render MobileHeaderComponent.new(title: "Complete Workout", close_modal: true) do |component| %>
  <% component.with_action do %>
    <% if @workout.in_progress? %>
      <%= form.submit "Finish", class: "btn-compact btn-primary" %>
    <% end %>
  <% end %>
<% end %>