<%# Header section %>
<div>
  <div class="flex justify-between items-center gap-2">
    <div>
      <div class="flex items-center gap-x-2">
        <%= link_to "←", workouts_path, class: "text-2xl" %>
        <h1 class="text-2xl font-bold"><%= workout.name %> <%= render_sequence_number(workout) %></h1>
      </div>
    </div>
    <div class="flex gap-2">
      <%= link_to edit_workout_path(@workout), class: "btn btn-icon btn-primary", data: { turbo_stream: true } do %>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
          <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z"/>
        </svg>
        <span>Edit</span>
      <% end %>
    </div>
  </div>
  <p class="text-gray-600"><%= workout.date.strftime("%B %d, %Y") %></p>
</div>
