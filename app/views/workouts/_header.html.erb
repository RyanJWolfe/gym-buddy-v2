<%# Header section %>
<div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-4">
  <div>
    <h1 class="text-2xl font-bold text-gray-900"><%= workout.name %></h1>
    <p class="text-gray-600"><%= workout.date.strftime("%B %d, %Y") %></p>
  </div>
  <div class="flex gap-2 w-full sm:w-auto">
    <%= link_to edit_workout_path(workout), class: "flex-1 sm:flex-none btn btn-primary" do %>
      <span>Edit Workout</span>
    <% end %>
    <%= link_to workouts_path, class: "flex-1 sm:flex-none btn btn-tertiary" do %>
      <span>Back</span>
    <% end %>
  </div>
</div> 