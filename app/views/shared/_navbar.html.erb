<nav class="bg-gray-800 text-white">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-3">
      <div class="flex items-center space-x-4">
        <%= link_to "GymTracker", root_path, class: "text-xl font-bold" %>
        
        <% if user_signed_in? %>
          <div class="hidden md:flex space-x-4">
            <%= link_to "Dashboard", dashboard_path, class: "hover:text-indigo-200" %>
            <%= link_to "Workouts", workouts_path, class: "hover:text-indigo-200" %>
            <%= link_to "Exercises", exercises_path, class: "hover:text-indigo-200" %>
            <%= link_to "Profile", profile_path, class: "hover:text-indigo-200" %>
          </div>
        <% end %>
      </div>
      
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <span class="hidden md:inline"><%= current_user.email %></span>
          <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "bg-gray-700 px-3 py-1 rounded hover:bg-gray-600" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "hover:text-indigo-200" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "bg-indigo-700 px-3 py-1 rounded hover:bg-indigo-800" %>
        <% end %>
      </div>
    </div>
    
    <% if user_signed_in? %>
      <div class="md:hidden pb-3">
        <div class="flex space-x-4">
          <%= link_to "Dashboard", dashboard_path, class: "hover:text-indigo-200" %>
          <%= link_to "Workouts", workouts_path, class: "hover:text-indigo-200" %>
          <%= link_to "Exercises", exercises_path, class: "hover:text-indigo-200" %>
          <%= link_to "Profile", profile_path, class: "hover:text-indigo-200" %>
        </div>
      </div>
    <% end %>
  </div>
</nav> 